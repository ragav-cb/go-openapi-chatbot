# 🧠 AI Help Chatbot (Go + React + OpenAI)

A full-stack chatbot that uses OpenAI's GPT to respond to user messages. Built with:

- Go backend
- React frontend
- Docker & Docker Compose

## 🧪 Local Development

1. Clone the repo
2. Set your OpenAI API key in `backend/.env`

